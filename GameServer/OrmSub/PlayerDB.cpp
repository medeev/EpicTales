

#include "Pch.h"
#include "PlayerDB.h"

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief  캐쉬에 반영한다.
////////////////////////////////////////////////////////////////////////////////////////////////////
void PlayerDB::applyCache(DBOrm::OrmObjectPtr origin) const
{
	switch (getCrudState())
	{
	case DBOrm::OrmObject::CRUD::Insert:
		{
		}
		break;

	default:
		break;
	}
}

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief  클론한다.
////////////////////////////////////////////////////////////////////////////////////////////////////
std::shared_ptr<PlayerDB> PlayerDB::clone() const
{
	auto clone = std::make_shared<PlayerDB>();
	copyMember(*clone);
	return clone;
}

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief  위치를 반환한다.
////////////////////////////////////////////////////////////////////////////////////////////////////
Vector PlayerDB::getLocation() const
{
	return Vector(getX(), getY(), getZ());
}

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief  방향을 반환한다.
////////////////////////////////////////////////////////////////////////////////////////////////////
Vector PlayerDB::getDir() const
{
	return Vector::FromYaw(getYaw());
}
